[package]
name = "naui-gtk"
description = "naui の Linux バックエンド (GTK4 / libadwaita) の骨組み。未実装"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
naui-core.workspace = true
# gtk4 / libadwaita への依存は、実装に着手する時点で追加する。
# 現状は API の形だけを定義した骨組みのため、依存を持たない。
